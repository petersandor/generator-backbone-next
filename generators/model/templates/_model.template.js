import * as Backbone from 'backbone';
import { defaults } from 'backbone-decorators';
import * as _ from 'underscore';

// @defaults({})
class <%= name %>Model extends Backbone.Model {

}

export default <%= name %>Model;
